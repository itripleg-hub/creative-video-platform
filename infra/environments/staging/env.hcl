# -----------------------------------------------------------------------------
# staging environment — shared locals
# -----------------------------------------------------------------------------
locals {
  environment = "staging"
  project     = "creative-video"
  region      = "us-east-1"
  aws_account = "222222222222" # replace with your actual staging account ID
}
